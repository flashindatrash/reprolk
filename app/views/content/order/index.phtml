<div class="panel panel-default">
	<div class="panel-heading"><?php print sprintf($this->str('all_orders_result'), count($this->orders)); ?>
		<div class="pull-right">
			<?php print View::link('OrderAdd', '', 'add', 'btn btn-xs btn-primary glyphicon glyphicon-plus', Application::str('tooltip_order_add'), null, 'left'); ?>
			<?php print View::link('OrderView', '', 'view', 'btn-action btn btn-xs btn-primary glyphicon glyphicon-eye-open disabled', Application::str('tooltip_order_view'), null, 'top'); ?>
			<?php print View::link('OrderEdit', '', 'edit', 'btn-action btn btn-xs btn-primary glyphicon glyphicon-pencil disabled', Application::str('tooltip_order_edit'), null, 'top'); ?>
			<?php print View::link('OrderDuplicate', '', 'duplicate', 'btn-action btn btn-xs btn-primary glyphicon glyphicon-duplicate disabled', Application::str('tooltip_order_duplicate'), null, 'right'); ?>
		</div>
	</div>
	
	<table class="table table-hover">
		<thead>
			<tr>
				<?php
				foreach ($this->fields as $th) {
					print '<th>' . $this->str('order_' . $th) . '</th>';
				}
				print '<th>' . $this->str('username') . '</th>';
				?>
			</tr>
		</thead>
		<tbody>
			<?php
			$orders = '';
			foreach ($this->orders as $n => $order) {
				$orders .= '<tr class="order_row noselect cursor_pointer" id="' . $order->id . '">';
				foreach ($this->fields as $td) {
					$orders .= '<td>' . $order->$td . '</td>';
				}
				$orders .= '<td>' . $order->username . '</td>';
			}
			print $orders;
			?>
		</tbody>
	</table>
</div>
