<div class="panel panel-default">
	<div class="panel-heading"><?php print sprintf($this->str('all_orders_result'), count($this->orders)); ?>
		<div class="pull-right">
			<a id="add" href="/order/add" class="btn btn-xs btn-primary glyphicon glyphicon-plus" data-toggle="tooltip" data-placement="left" title="<?php print $this->str('tooltip_order_add'); ?>"></a>&nbsp;
			<a id="view" href="#" class="btn-action btn btn-xs btn-primary glyphicon glyphicon-eye-open disabled" data-toggle="tooltip" data-placement="top" title="<?php print $this->str('tooltip_order_view'); ?>"></a>&nbsp;
			<a id="edit" href="#" class="btn-action btn btn-xs btn-primary glyphicon glyphicon-pencil disabled" data-toggle="tooltip" data-placement="top" title="<?php print $this->str('tooltip_order_edit'); ?>"></a>&nbsp;
			<a id="duplicate" href="#" class="btn-action btn btn-xs btn-primary glyphicon glyphicon-duplicate disabled" data-toggle="tooltip" data-placement="right" title="<?php print $this->str('tooltip_order_duplicate'); ?>"></a>&nbsp;
		</div>
	</div>
	
	<table class="table table-hover">
		<thead>
			<tr>
				<?php
				foreach ($this->fields as $th) {
					print '<th>' . $this->str('order_' . $th) . '</th>';
				}
				print '<th>' . $this->str('username') . '</th>';
				?>
			</tr>
		</thead>
		<tbody>
			<?php
			$orders = '';
			foreach ($this->orders as $n => $order) {
				$orders .= '<tr class="order_row noselect cursor_pointer" id="' . $order->id . '">';
				foreach ($this->fields as $td) {
					$orders .= '<td>' . $order->$td . '</td>';
				}
				$orders .= '<td>' . $order->username . '</td>';
			}
			print $orders;
			?>
		</tbody>
	</table>
</div>
